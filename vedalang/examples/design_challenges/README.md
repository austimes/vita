# Design Challenge Examples

Use this folder for incremental DSL design challenges (for example `dc*` style challenge models).

Typical contents:
- challenge-specific models
- variants used to test language expressiveness
