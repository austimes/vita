# MiniSystem Examples

Incremental v0.2 MiniSystem progression.

Notes:
- The sequence still grows from `minisystem1` through `minisystem8`, but it now
  uses only the current v0.2 object model.
- The later steps use temporal reference data, multi-region runs, and networks
  instead of the removed public timeslice/scenario surface.
