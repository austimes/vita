# Minimal VedaLang example: A simple thermal power plant
# Uses new VedaLang syntax (P4): process_roles, process_variants, availability

model:
  name: MiniModel
  description: A minimal test model with one power plant

  regions:
    - REG1

  milestone_years: [2020, 2030]

  commodities:
    - id: secondary:electricity
      type: energy
      unit: PJ
      description: Electricity

      combustible: false
    - id: primary:natural_gas
      type: fuel
      unit: PJ
      description: Natural Gas

      combustible: true
      lhv_mj_per_unit: 50.0
      hhv_mj_per_unit: 55.0
process_roles:
  - id: generate_electricity
    activity_unit: PJ
    capacity_unit: GW
    stage: conversion
    required_inputs:
      - commodity: primary:natural_gas
    required_outputs:
      - commodity: secondary:electricity

process_variants:
  - id: ccgt
    role: generate_electricity
    inputs:
      - commodity: primary:natural_gas
        basis: HHV
    outputs:
      - commodity: secondary:electricity
    efficiency: 0.55

availability:
  - variant: ccgt
    regions: [REG1]
